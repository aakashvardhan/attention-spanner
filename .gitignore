node_modules/
dist/
release/
.DS_Store
*.local
.claude/settings.local.json

# Local env / Firebase web config (never commit secrets or keys)
.env
.env.*
!.env.example

# Google Calendar OAuth signing key (see docs/google-calendar-setup.md)
gcal-key.pem

# Firebase Functions build output + emulator state (WhatsApp bridge)
functions/lib/
.firebase/
firebase-debug.log
firestore-debug.log

# iOS Swift package build artifacts
ios/**/.build/
demo/.profile/
demo/.profile-probe/
demo/out/
