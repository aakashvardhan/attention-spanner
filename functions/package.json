{
  "name": "adhd-reader-functions",
  "description": "WhatsApp bridge: Meta Cloud API webhook writing to the extension's Firestore sync collections",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "lib/functions/src/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions,firestore",
    "deploy": "npm run build && firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.0.0"
  },
  "devDependencies": {
    "typescript": "~5.8.0"
  }
}
